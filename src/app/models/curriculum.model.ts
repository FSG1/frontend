// The curriculum is used to filter on in  module.component.ts
// an array of this model is returned by getCurricula method in backend.service.ts
export interface Curriculum {
  name: string;
  code: string;
  id: number;
}
